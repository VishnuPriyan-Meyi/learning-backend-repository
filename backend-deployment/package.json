{
  "name": "backend-deployment",
  "version": "1.0.0",
  "description": "Learning Backend — AWS Lambda Function",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20"
  }
}
